<IfDefine HAVE_SCHEMA>
    <IfModule !mod_schema.c>
	LoadModule schema_module	extramodules/mod_schema.so
    </IfModule>
</IfDefine>

<IfModule mod_schema.c>

    # SchemaProcess - W3C Schema validation process

    # HttpProxy - http proxy for schema processing

</IfModule>
